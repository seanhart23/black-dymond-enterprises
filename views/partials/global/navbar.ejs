<nav class="navbar position-fixed navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
        <a class="navbar-brand" href="#">APBDE Dymond Portal</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav ml-auto mb-2 mb-lg-0">  
                <% if(currentUser){ %>
                    <li class="nav-item">
                        <a class="nav-link disabled" id='welcome' href="#" tabindex="-1" aria-disabled="true">Welcome, <%= currentUser.firstName %> <%= currentUser.lastName %></a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/logout"><i class="fas fa-sign-out-alt"></i></a>
                    </li>
                <% } else { %>
                <% } %>         
            </ul>
        </div>
    </div>
</nav>